sudo: false

language:
  - c
  - cpp
compiler:
  - gcc

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - george-edison55-precise-backports # cmake 3.2.3 / doxygen 1.8.3
    packages:
      - cmake
      - cmake-data

before_install:
  - pip install --user cpp-coveralls

script:
  - git submodule update --init --recursive
  - mkdir build
  - cd build
  - cmake ..
  - make
  - ./tests/crypto_test/run_tests
  - cd ..

after_success:
  - coveralls --exclude tests --exclude build --gcov-options '\-lp'

notifications:
  email: false
